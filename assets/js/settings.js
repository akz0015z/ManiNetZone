// assets/js/settings.js
// Here you'll add all the logic for:
// - loading current user profile
// - saving theme/background
// - uploading avatar to Firebase Storage
// - changing display name / username with cooldowns
// - changing password, email
// - toggling 2FA
// - logging out of all sessions

console.log("Settings JS loaded");

// Example stub:
window.loadUserProfile = async function (user) {
  // This gets called from auth.js after auth state is ready
  // Fetch user doc from Firestore and fill in:
  // currentDisplayName, currentUsername, currentEmail, settingsAvatar, etc.
};
